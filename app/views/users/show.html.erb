<h2><%= @user.name %></h2>

<% if @user == current_user %>
  <%= link_to "Update My Profile", edit_user_path(@user) %>
<% end %>

<% @posts.each do |post| %>
  <%= render partial: 'posts/list_show', locals: {post: post} %>
<% end %>
